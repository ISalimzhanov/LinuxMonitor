package database;

// Enum for process properties
public enum ProcessProperties {
    NAME,
    CPU,
    TIME,
    USER,
    MEMORY_UTIL,
    STAT,
    RSS,
    TTY,
    PID,
    START,
    VSZ,
    COMMAND
}
